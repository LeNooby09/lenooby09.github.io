---
import Dither from '../blocks/Backgrounds/Dither/Dither.tsx';
import TextBox from '../blocks/TextBox/TextBox.tsx';
---

<div style={{width: '100%', height: '100%', position: 'absolute', top: 0, left: 0}}>
	<Dither client:load/>
</div>

<TextBox
  client:load
  title="LeNooby09's site"
  content="This is an example text that sits on top of the dither background without interfering with its reactivity."
/>
